# Benchmarks

Add benchmarks only after the corresponding behavior is correct and covered by tests. Record
the machine, compiler, configuration, input shape, and comparison baseline with every result.
